# PostgreSQL configuration for external connections
# This file ensures the database accepts connections from any host

# Listen on all addresses
listen_addresses = '*'

# Connection settings
max_connections = 200
shared_buffers = 256MB

# Logging
log_connections = on
log_disconnections = on
log_statement = 'all'

# Authentication
password_encryption = md5